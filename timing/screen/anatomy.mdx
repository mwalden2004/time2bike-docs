---
title: "A tour of the screen"
description: "Every part of the timing screen, top to bottom, and what it does."
icon: "table-columns"
---

Open your event and click **Live Timing**. This is what you're looking at.

<Frame caption="Demo data.">
  <img src="/images/timing/operator-anatomy.png" alt="The timing screen with fictional riders, race controls, race board, and footer" />
</Frame>


1. **Top bar** — status, clock, sync, help, setup, and race selection.
2. **Race clock and controls** — start, pause, stop, and the next-action prompt.
3. **Bib input** — type a number to record at the selected point.
4. **Riders** — search and filter the selected race's roster.
5. **All races** — check and control the other races.
6. **Timer setup** — open the device and race settings.
7. **Footer** — more actions, live sharing, and finalization.

## Top bar

| What | What it tells you |
|---|---|
| **Race tabs** | Every race in the event. The highlighted one is where your entries go. Tap to switch — the controls below change to suit that race's format. |
| **Status badge** | Whether the timer is running, paused, stopped, finalized, or published. |
| **Clock chip** | How closely this device's clock agrees with Time2Bike's. The value estimates clock-sync uncertainty; a large value needs investigation before close results are accepted. |
| **Sync chip** | 🟢 synced, 🟡 *N* queued, 🔴 offline. Tap it to see exactly what's waiting to upload. |
| **Help** | The full operator reference, built into the screen. |

<Tip>
  Check the race tab before every race to avoid assigning a crossing to the
  wrong race.
</Tip>

## The race prompt

The race clock panel tells you what to do next: press **START RACE** at the gun,
then enter a plate or tap a rider. It changes when the race stops, so read that
prompt if you are unsure what the screen will record.

## The race board

The **All races** panel below the rider list shows what every race is doing: running with a live clock, stopped with a finisher count, or not started.

<Frame caption="Demo data.">
  <img src="/images/timing/race-board.png" alt="Race board showing three races running and Junior waiting to start" />
</Frame>

- The header says **3 of 4 running**, so you never have to click through
  tabs to find out what's live.
- **Start or stop any race from here**, without leaving the one you're recording
  into.
- Tap a race name to switch to it. The row you're recording into is badged
  **recording**, and defers to the big buttons below rather than offering a
  second set.

It collapses, and starts collapsed when the event has only one race.

## Start / Pause / Stop

Big buttons for the race you're recording into, sized for cold hands.

- **START RACE** — opens the clock. The label underneath tells you what pressing
  it actually does to the field: *all 47 riders go on course*, or *riders go on
  course as you check them in*.
- **Pause / Resume** — labelled **Freeze clock** or **Pause entries** depending
  on how the timer is set. Freezing stops everyone's elapsed time (a real course
  hold); pausing entries just stops recording while the clock runs on.
- **STOP** — ends the race. Permitted result corrections remain available before finalization.
- **Set the start time** — the small button under the grid. Puts the start at a
  time you type off the clock instead of the moment you press a button, for a
  race that went while nobody was at the screen. Once a race is running the same
  button reads **Correct the start time**.

Pressing START on a race that's already running says **RESTART RACE** instead.
Only do that if you fired the previous start by mistake, because it invalidates
the finishes already recorded against the old clock. If the start merely
happened *earlier* than you pressed it, use **Correct the start time** instead:
that keeps every finish and re-times them.

Interval-start and dual-slalom races don't show this grid; their starts happen
per rider or per lane.

## The big number box

The single most important control. Type a bib, press **Enter**.

- It stays focused, so you don't have to click it before typing.
- As you type, matching riders appear as chips. Tap one instead of finishing
  the number.
- An **UNDO** button sits next to it. It identifies the supported recent action it can reverse, subject to your role.

What Enter *does* depends on your recording point: finish, lap, split, or
check-in. See [Recording points](/timing/screen/recording-points).

## Riders

The **Riders** panel holds the selected race's roster. Use **On course** to see
who has started without a recorded finish, or **Finished** to check finishers.
The other filters show riders who have not started and DNF/DNS/DSQ outcomes.

Tap a rider's name to record them when their bib is unreadable. Use the search
box to find a plate or name, and **Hide** to collapse the panel.

## Any result row

Use the three-dot button at the end of a rider row to open its menu: manual time, DNF/DNS/DSQ, edit splits, void a lap, or delete time. What you see depends on your access level.

→ [Corrections](/timing/screen/corrections)

## Timer setup

Choose **Setup** in the top bar, or **Timer setup** below the rider lists. The
panel holds the timer settings in three sections.

<Frame caption="Demo data.">
  <img src="/images/timing/timer-setup.png" alt="Timer setup with How I am timing selected" />
</Frame>

| Section | What's in it |
|---|---|
| **This device** | Where you're standing, and whether that follows you between races. Saves as you tap → [Recording points](/timing/screen/recording-points) |
| **How I am timing** | One race at a time vs one shared chute · how precisely times are shown · what pause means. Plus the way into wave setup and operator links |
| **Races** | Pick **one race, several, or all of them**, then set laps, timing points on course, and whether riders start at the gun or check in |

### Setting up several races at once

Five criterium categories usually want identical settings, and doing them one at
a time is how the fourth ends up different. Tick as many races as you like, or
**All races**, and set them together.

Review these details before saving:

- **Only what you change gets written.** Anything you don't touch is left exactly
  as it is on each race. Selecting five races and editing the lap count won't
  flatten their timing points.
- **Where the selected races disagree, the field shows `mixed`** and stays that
  way until you set it. Nothing picks a value on your behalf.

Fields you have changed are badged **will change**, so you can see what's about to
be written before you press save. The button names its target: *Save to 5 races*.

Changing the selection mid-edit keeps your pending changes and applies them to
whatever is selected when you save. Untouched fields re-read from the new
selection.

There's also **…and the default for any race added later**, which writes the same
settings to the fallback new races inherit.

Anything that talks to a piece of hardware, like the RFID pad or the laser
boxes, stays on the timing screen itself, because it has to run on the device
you're standing at.

<Tip>
  **Setup** in the top bar is also available on the interval-start, dual-slalom,
  and combined screens. **More actions → Timer setup** opens the same panel.
</Tip>

## Footer

| Button | What it does |
|---|---|
| **More actions** | Exports, imports, add riders, PDFs, reset — plus a second way into **Timer setup**, for the screens that don't show its button |
| **Share live results** | Opens a public spectator link that updates as you time |
| **Finalize Session** | Locks the results |
| **Results PDF** / **Publish to public** | Appear once you've finalized |

## Inside More actions

Worth knowing what's in there before you need it:

<AccordionGroup>
  <Accordion title="Download for offline" icon="download">
    Caches timing-page assets on supported browsers. Do this ahead of time
    and test reopening the same timer without a connection.
  </Accordion>
  <Accordion title="Register a rider" icon="user-plus">
    Open the event's registration form for a day-of entry. Needs a connection. → [Adding riders](/timing/screen/adding-riders)
  </Accordion>
  <Accordion title="Exports and printing" icon="file-export">
    CSV, JSON, printed results, per-race PDF, whole-event PDF, whole-event CSV.
    Printing and the CSV/JSON exports work offline.
    → [Exports](/timing/results/exports)
  </Accordion>
  <Accordion title="Operator tokens" icon="qrcode">
    Create and revoke volunteer links.
    → [Volunteer links](/timing/crew/tokens-and-qr)
  </Accordion>
  <Accordion title="Record prime" icon="award">
    Award a mid-race prime to a rider on a given lap.
    → [Laps & primes](/timing/formats/laps-and-primes)
  </Accordion>
  <Accordion title="Import results" icon="upload">
    Pull results in from another timing platform's file.
  </Accordion>
  <Accordion title="Reset timing data" icon="triangle-exclamation">
    Removes timing data for one race or the whole event. Export records you
    need first; **UNDO** does not reverse a reset. The dialog checks this
    device’s queue, so confirm other devices have synced too. Use it only
    when you intend to remove the selected records, such as after a test run.
  </Accordion>
</AccordionGroup>

## Next

<Columns cols={2}>
  <Card title="Recording points" icon="map-pin" href="/timing/screen/recording-points">
    Telling the screen where on the course you're standing.
  </Card>
  <Card title="Corrections" icon="pen" href="/timing/screen/corrections">
    Fixing what goes wrong.
  </Card>
</Columns>
