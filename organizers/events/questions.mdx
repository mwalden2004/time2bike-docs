---
title: "Custom questions"
description: "Ask riders extra info during registration."
icon: "circle-question"
---

Add custom questions that every registrant for this event must answer during checkout. Manage them at **Event → Questions**.

![Questions tab](/images/orgs/questions.png)

## Supported types

| Type | Use it for |
|---|---|
| **text** | Short free-form answers (jersey number, club). |
| **textarea** | Long free-form (allergies, notes). |
| **select / radio** | Single choice from a list. |
| **checkbox** | Yes/no acknowledgements. |
| **number** | Numeric answer. |
| **email** / **phone** | Pre-validated formats. |

## Add a question

Click **New question** and fill in:

- **Text** — the prompt riders see.
- **Description** — optional helper text under the prompt.
- **Type** — see above.
- **Required** — toggle to block checkout until answered.
- **Placeholder** — hint text inside the input.
- **Options** — for `select` and `radio`, one per row.

## Order

Drag the handle on each row to reorder — the whole new order is saved in one go,
so moving a question from the bottom of a long list to the top is one action.
The up/down arrows on each row do the same thing a step at a time, and are the
keyboard and screen-reader path.

Order is preserved on the rider's registration screen. If someone else reordered
the list while you had it open, the save is refused rather than silently
dropping their question out of the order — reload and try again.

{/* IMAGE NEEDED: /events/[slug]/register → Questions step rendering a select and a checkbox */}

## Answers

Riders' answers appear on the registration record (see [Event registrations](/organizers/registrations/event-registrations)) and are exportable from there.
