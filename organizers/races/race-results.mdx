---
title: "Race results"
description: "Manual result entry, bulk paste, CSV upload."
icon: "trophy"
---

**Race → Results** is for organizers who time manually or who want to publish results without using [Live Timing](/timing/overview).

{/* IMAGE NEEDED: /organizations/[id]/events/[eventId]/races/[raceId]/results → results table with three upload modes */}

## Three input modes

Click **Add results** and pick one:

### Single rider

1. Search for the rider by name.
2. Enter their **finish time** (mm:ss.SSS) or pick a **status** (`dnf`, `dns`).
3. Save.

### Bulk paste

Paste lines from a spreadsheet. Format per line:

```
[bib],[time-or-status]
```

For example:

```
12,1:23:45.678
14,dnf
20,01:24:01.500
```

Time2Bike matches bibs to registered riders. Unmatched bibs show in a "needs attention" callout.

### CSV upload

Same shape as bulk paste but as a file. Header row optional; columns: `bib`, `time`, `status`, `categoryName` (optional override).

## Split results

If the race is set to score age brackets or men and women separately, results
are grouped accordingly everywhere they appear — this table, live results, the
public results page, and the printed PDF. Each group gets its own placement and
its own time gaps.

You don't enter anything differently. Everyone is one race with one set of
finish times; the split only changes how they're scored and displayed.

→ [Splitting one race into several results](/organizers/races/race-overview#splitting-one-race-into-several-results)

## Display & publish

Results are **draft** until you click **Publish**. Drafts are visible only to
your own organization; once published they appear on your public event page.

{/* IMAGE NEEDED: time2.bike/events/[slug]/results → published race results table */}

If you used Live Timing for this race, results appear here automatically once you finalize (see [Finalize & publish](/timing/results/finalize-and-publish)).
