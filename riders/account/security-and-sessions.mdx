---
title: "Security & sessions"
description: "Change your password, manage MFA, and sign out active devices."
icon: "lock"
---

## Change your password

At `time2.bike/account/settings/security`:

<Steps>
  <Step title="Enter your current password">
    Enter your current password to authorize the change.
  </Step>
  <Step title="Enter a new password">
    Minimum 8 characters. Time2Bike doesn't enforce specific complexity
    rules — pick something long and unique.
  </Step>
  <Step title="Confirm the new password">
    Type it again to avoid typos.
  </Step>
  <Step title="Click Update Password">
    On success, **every other session** on your account is signed out and
    your current session keeps a fresh token. Other devices need to sign
    in again.
  </Step>
</Steps>

<Frame caption="Password settings and text-message and email authentication controls. Demo data.">
  <img src="/images/riders/security.png" alt="Password settings and text-message and email authentication controls." />
</Frame>

## Multi-factor authentication (MFA)

Time2Bike supports two MFA methods:

- **Text message (SMS)** — code sent to a verified phone number
- **Email** — code sent to your account email

### Turning on SMS MFA

<Steps>
  <Step title="Verify a phone first">
    Add and verify a phone in [Profile](/riders/account/profile).
  </Step>
  <Step title="Pick the phone">
    On the Security page, select the phone from the dropdown.
  </Step>
  <Step title="Click Enable SMS 2FA">
    This enables text-message codes for sign-in using the verified phone you selected.
  </Step>
</Steps>

### Turning on email MFA

Click **Enable Email 2FA** to use your verified account email for sign-in codes.

You can have both methods on at once; sign-in will let you pick.

### Turning MFA off

Click **Disable SMS 2FA** or **Disable Email 2FA**. If you turn off the
**only** active method, MFA is fully disabled and sign-in becomes a
single-step password again.

## Active sessions

`time2.bike/account/settings/sessions` lists your account's active sign-in
sessions. Device descriptions come from the browser information sent at sign-in.

Each session shows:

- The **device** (browser + OS, parsed from user agent)
- The **IP address**
- The **last active** timestamp

<Frame caption="Active browser sessions with example device information and documentation IP addresses. Demo data.">
  <img src="/images/riders/sessions.png" alt="Active browser sessions with example device information and documentation IP addresses." />
</Frame>

Click the trash icon (**Revoke session**) beside a device to sign it out.
When several sessions are listed, **Sign out all devices** ends them all.

Use this if you signed in on a shared computer or lost a device.
